# 《野球魂：白球のキセキ》×《實況野球：栄冠ナイン》深度研究與 `chouten-court` 優化計畫

> 研究基準日：2026-08-21
>
> 研究範圍：《プロ野球スピリッツ2024-2025／2026》的「白球のキセキ」、《パワフルプロ野球2024-2025／2026-2027》的「栄冠ナイン」，以及目前 `chouten-court` 的規格、原始碼與本機實際畫面。
>
> 證據原則：外部模式的功能事實只採 KONAMI 官方網站、官方 FAQ、官方更新公告與官方公司新聞；畫面判讀標為「官方截圖觀察」，設計結論標為「本研究推論」。
>
> 目的：找出可轉譯成台灣高中籃球經營遊戲的設計原則，不複製 KONAMI 的數值、文字、美術資產、人物肖像或介面。

## 1. 結論先行

目前專案不是缺少系統，而是缺少「讓系統彼此咬合、被玩家感受到」的中間層。

現有版本已經具備完整可玩的骨架：每週訓練／休養／練習賽、事件、正式賽、先發與輪替、疲勞與傷病、招生畢業、聲望、球員個性、賽前預覽、賽後摘要、單季與生涯總結。這些功能與兩款參考模式的核心方向相符，不應推倒重做。

真正的差距集中在五點：

1. **週決策仍偏單薄**：目前多數週只選一項全隊屬性，沒有月度方向、球員個別重點、行程連鎖或資源機會成本。
2. **比賽缺少教練的臨場存在感**：賽前可排陣與選兩組戰術，但按下開打後直接得到結果，最重要的籃球情緒沒有被演出。
3. **長期世界缺少可累積資產**：聲望會影響招生，但尚未形成設施、教練專長、球探範圍、宿敵、校史與歷史球隊的正向循環。
4. **資訊透明，但敘事回饋不足**：數值預覽做得比一般 MVP 完整；然而訓練、事件、比賽與畢業後，玩家仍不容易回答「為什麼這一週值得記住」。
5. **畫面有一致性，但辨識度不足**：深色卡片、橘色重點與 DiceBear 頭像已形成基礎風格；目前仍像乾淨的管理工具，還不像有球場氣味、校隊文化與大賽張力的籃球作品。

因此最有效的順序不是先做更多屬性、完整對手名冊或 3D 比賽，而是：

```text
視覺層級與賽程可讀性
→ 結果原因與球員成長敘事
→ 月度方向＋小量個別指導
→ 關鍵回合教練決策
→ 跨年度學校資產、宿敵與校史
→ 三年短局／長期王朝與收藏層
```

## 2. 研究邊界與版本差異

### 2.1 白球のキセキ

- 《プロ野球スピリッツ2024-2025》是「白球のキセキ」首次登場的作品；2025 年 Ver.2.0.0 曾針對日程快轉、戰績點、球探、商店、特殊能力與宿敵保護等長期遊玩摩擦做集中改善。[官方模式頁](https://www.konami.com/games/prospi/2024-2025/)、[Ver.2.0.0 更新](https://www.konami.com/games/prospi/2024-2025/update/v200)
- 最新正式作品《プロ野球スピリッツ2026》於 2026-07-16 發售；本代的白球模式加入高中棒球 DH 與投手連投負荷等規則。不能把 2026 新功能倒推成 2024 原始版本就有。[2026 官方網站](https://www.konami.com/games/prospi/2026/jp/ja/)、[DH FAQ](https://www.konami.com/games/2026_support/faq/0/jp/ja/ps5/item?no=264)、[連投 FAQ](https://www.konami.com/games/2026_support/faq/0/jp/ja/ps5/item?no=266)

### 2.2 栄冠ナイン

- 本研究以家用版《パワフルプロ野球2024-2025》為核心比較，並把《2026-2027》的三年模式、三種比賽風格、新生重選、宿敵編輯與舊隊回歸視為最新演進。[2024-2025 官方模式頁](https://www.konami.com/pawa/2024-2025/mode/eikan)、[2026-2027 官方模式頁](https://www.konami.com/pawa/2026-2027/mode/eikan)
- 《栄冠ナイン クロスロード》是另外一款帶角色收集、成長板、跨玩家內容與服務型節奏的衍生作，不等於家用版本傳。本文只借鏡「畢業成果回流」與「歷史隊伍再利用」，不建議移植能量、每日任務或限時素材。[官方發表](https://www.konami.com/games/corporate/ja/news/topics/20230803/)、[世界の栄冠更新](https://www.konami.com/games/corporate/ja/news/topics/20240313/)

## 3. 兩個模式共同解決的核心問題

兩者表面上一個寫實、一個超變形，但底層都在處理相同的高中球隊經營問題：

```text
短期：這週做什麼、這場怎麼打、誰該上場
  ↓
中期：這一季能走多遠、傷疲與接班如何平衡
  ↓
長期：三年級離隊後，學校留下什麼、下一屆為何更值得玩
```

共同成功點不是「很多數值」，而是以下四條可見因果鏈：

1. **能力會改變可選策略**，不是只讓總戰力變大。
2. **日程選擇會排擠其他機會**，休息、指導、球探與訓練都有時間成本。
3. **上場本身也是養成**，現在的勝率與下一屆成熟度形成衝突。
4. **球員會離開，但學校與世界會記住他們**，失敗季仍能產生長期價值。

## 4. 白球のキセキ：值得借鏡的設計

### 4.1 長期主角是「學校」，不是某一名球員

官方明示玩家以高中棒球監督身分挑戰甲子園；新生加入與三年級畢業會反覆循環，遊玩年數無上限。新生中還可能出現模仿職棒選手動作與能力的「プロ選手憧れ部員」。[官方模式說明](https://www.konami.com/games/prospi/2024-2025/)

這帶來三層目標：

- 球員層：能力、信賴、狀態、球探評價與畢業成果。
- 球隊層：當屆陣容、戰術相性、賽程與投手調度。
- 學校層：評判、設備、宿敵、圖鑑與跨年度資源。

**對本專案的轉譯**：球員是三年期角色，真正的永久進度應由學校聲望等級、訓練環境、球探網、教練專長、宿敵紀錄與校史構成。

### 4.2 能力、特性與戰術相性會直接改變選擇

白球模式不是把戰術卡當成固定按鈕。官方 FAQ 說明：

- 部分戰術只在攻擊、防守或特定條件下出現。[白球 FAQ 一覽](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/category?category=9)
- 球員基礎能力或特殊能力與戰術相容時，戰術卡會出現 `△` 並獲得強化。[戰術卡 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=149)

**設計推論**：好的特性不是被動加 3%，而是讓玩家看見「這名球員使某個選擇變好」。

**對本專案的轉譯**：

- 隊長型不只加總戰力，還可強化「穩住節奏」或「暫停後第一回合」指令。
- 抗壓型不只在八強／四強加成，還可在關鍵回合解鎖或提高某類戰術可信度。
- 飆分型可強化「提速追分」，但同時提高失誤或體力成本。
- 當選項獲得加成時，必須直接標示來源，例如「張俊傑：飆分型相性 +1」，不能只讓公式暗中生效。

### 4.3 訓練、比賽起用與設備形成競爭資源

官方資料確認：

- 相同練習連續執行等特殊狀況會顯示 `△`，表示效果提高。[練習指令 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=142)
- 練習器材具有耐久度並可在設備頁查看。[設備耐久 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=145)
- 比賽起用會比較容易提高信賴度；比賽活躍或道具會提高球探評價。[信賴度 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=136)、[球探評價 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=135)

**對本專案的轉譯**：先加入「訓練連鎖、個別指導名額、實戰信任／熟練」即可形成決策，不必立刻加入設備耐久。現有規格已把耐久列為延後項目，這個取捨正確；耐久若沒有維修、預算與不同設施選擇，只會增加雜務。

### 4.4 比賽控制提供不同投入深度

白球模式官方提供監督戰術、全隊動作操作與單一球員操作；玩家可以依自己想投入的程度選擇比賽方式。[官方模式說明](https://www.konami.com/games/prospi/2024-2025/)

2026 版又把跨場管理加深：前一日投球數會影響連投投手的調子，體力消耗延續到隔日，恢復量則受恢復能力影響。[連投 FAQ](https://www.konami.com/games/2026_support/faq/0/jp/ja/ps5/item?no=266)

**對本專案的轉譯**：不需要做可直接操控球員的籃球動作遊戲；成本更合理的做法是提供三種觀賽深度：

1. `快速結果`：沿用目前全自動模擬。
2. `關鍵回合`：每場出現 2–4 次教練決策，處理追分、犯規、換防、暫停與最後一擊。
3. `文字實況`：顯示節比分數、領先變化與關鍵球員，不要求逐球動畫。

三種深度必須共用同一套比賽引擎與最終結果規則，避免變成三套難以維護的遊戲。

### 4.5 寫實感來自多層訊號，不只模型精度

KONAMI 對作品整體的呈現說明包含球場雷射掃描、會隨比賽熱度變化的觀眾與板凳反應、球場煙火、立體音響、即時擊球數據，以及高度可調整的人體、姿勢與表演。[《プロスピ2024-2025》官方網站](https://www.konami.com/games/prospi/2024-2025/)

這些是作品整體呈現能力，不代表白球模式每個管理畫面都使用全部效果。真正可借鏡的原則是：

```text
角色辨識 + 場地辨識 + 群眾反應 + 聲音 + 關鍵數據
= 比單純提高材質解析度更完整的臨場感
```

**對本專案的轉譯**：優先做球衣／背號／姿態、場館階段背景、觀眾密度、比分與關鍵數據；不要把第一個美術里程碑設成全 3D 球員。

## 5. 栄冠ナイン：值得借鏡的設計

### 5.1 日程不是空白等待，而是一張風險／收益地圖

2024-2025 官方說明以「進行圖示」推動日期與練習，停在不同格子會發生事件；2026-2027 進一步明示月初決定日常練習方向，再以進行圖示推進球員成長。[2024-2025 模式頁](https://www.konami.com/pawa/2024-2025/mode/eikan)、[2026-2027 模式頁](https://www.konami.com/pawa/2026-2027/mode/eikan)

2024 版新增「他校調查、球場整備、個別指導」三種進行圖示，使偵察、環境與個人成長和一般訓練共用時間資源。[2024-2025 模式頁](https://www.konami.com/pawa/2024-2025/mode/eikan)

**對本專案的轉譯**：不必照搬棋盤，可以改成更適合手機的「四週行程帶」：

```text
本月主題：外線投射
第 1 週  團隊訓練       已完成
第 2 週  個別指導       現在
第 3 週  練習賽（名門） 可預覽
第 4 週  恢復／事件     未知
```

玩家仍然一週做一個主要決策，但能看見未來、安排連鎖，也知道本週選擇放棄了什麼。

### 5.2 評判必須改變內容，不只改變數字

官方說明學校評判達到門檻後，外國人 OB 才可能以轉生留學生加入；一年級入學人數也會由弱小時 6 人增加到名門時 10 人，已調查的球探地區則有可視化。[2024-2025 模式頁](https://www.konami.com/pawa/2024-2025/mode/eikan)

**對本專案的轉譯**：聲望應同時影響：

- 招生候選數量。
- 候選品質分布。
- 可偵察區域或校際邀請。
- 練習賽對手層級。
- 學校可解鎖的設施與教練專長。
- 稀有故事型球員出現率。

目前聲望已影響候選品質，應保留既有公式並向「內容解鎖」擴張，不要另造第二條用途重疊的名聲貨幣。

### 5.3 管理資訊與比賽資訊應分開

2024 版把戰力拆成「總合」與「比賽」兩種顯示，並將板凳登錄擴充到 20 人，讓玩家更容易理解養成價值和當場可用戰力不同。[2024-2025 模式頁](https://www.konami.com/pawa/2024-2025/mode/eikan)

**對本專案的轉譯**：目前已有基準戰力、疲勞折損與實際陣容權重，方向正確。下一步應統一命名：

- `球隊底蘊`：全名冊長期能力，適合看接班與招生。
- `本場戰力`：只看先發、輪替、疲勞、傷勢、戰術與當場狀態。
- `化學反應`：位置平衡、熟練搭配、隊長／球風相性。

不要再加入一個沒有用途說明的總評。

### 5.4 挫折要可解釋，也要有保護帶

官方曾針對栄冠模式加入投手體力條、降低單局大量失分、極端失分時切入自動比賽，並調整早期抽籤、對手擊球、球員情緒與評判變動。[2024-10-21 更新](https://www.konami.com/pawa/2024-2025/update/241021)

**本研究推論**：這些改動共同處理三個問題：資訊不足、不可逆爆局，以及弱隊過早抽到碾壓級對手。

**對本專案的轉譯**：

- 第一年的資格賽避免抽到最高級距對手。
- 所有重大失敗都顯示前三項主要原因，而不是只顯示隨機結果。
- 關鍵回合提供一次「穩住比賽」的低上限安全選項，避免一個隨機事件完全抹掉一季規劃。
- 保留爆冷，但讓爆冷來自可理解的戰術、球員狀態與波動，而非純黑箱。

### 5.5 三年短局是入口，不是割裂的新遊戲

2026-2027 最新版增加固定三年的短局，第三年夏季大會後還能接續成普通長期模式；玩家也可選監督戰術、整隊操作或單一球員操作。另可在每年四月轉任新地區，原本培育的球隊日後可能以宿敵身分回歸。[2026-2027 模式頁](https://www.konami.com/pawa/2026-2027/mode/eikan)

**對本專案的轉譯**：

- `三年挑戰`：讓新玩家在約 60–90 分鐘內經歷招生、畢業、正式賽與總結。
- `王朝模式`：保留現有 18 屆保險上限，或奪冠後讓玩家選擇退休／繼續。
- 三年挑戰結束時可「繼續執教」，共用同一存檔結構。
- 已完成的短局隊伍可存成歷史隊，在新生涯作為宿敵或練習賽對手。

### 5.6 超變形美術的價值是可讀性與親和力

從 KONAMI 官方模式頁截圖可觀察到，栄冠模式延續 PowerPro 系列的超變形角色、明亮色塊與高密度資訊面板；2024 版加入經理外觀編輯，2026 版擴大容貌與宿敵校服編輯。[2024-2025 模式頁](https://www.konami.com/pawa/2024-2025/mode/eikan)、[2026-2027 模式頁](https://www.konami.com/pawa/2026-2027/mode/eikan)

**對本專案的轉譯**：不需要模仿 PowerPro 的人物比例。應保留目前親和角色方向，但建立自己的「台灣高中籃球」輪廓：球衣、背號、位置、膚色、髮型、護具、鞋色與姿態要比隨機臉孔更能辨識球員。

## 6. 目前專案內容盤點

> 實作狀態更新（2026-08-21）：本節 6.1／6.2 保留研究當時的基線與畫面觀察；後續已完成 V1、訓練卡池與第一版特殊能力系統。最新狀態以 6.3 與第 12 節為準。

### 6.1 已完成且應保留的優勢

| 現有能力 | 程式／文件證據 | 評估 |
|---|---|---|
| 48 週年度、26 週非賽季、五階段正式賽 | [`calendar.ts`](../src/domain/calendar.ts)、[`officialMatch.ts`](../src/domain/officialMatch.ts) | 已具備短中期節奏骨架 |
| 12 人名冊、三年級畢業與新生補位 | [`roster.ts`](../src/domain/roster.ts)、[`graduation.ts`](../src/domain/graduation.ts)、[`recruiting.ts`](../src/domain/recruiting.ts) | 世代循環已成立 |
| 9 張流動訓練卡池、點數預算與每週最多 3 張 | [`trainingCardPool.ts`](../src/domain/trainingCardPool.ts)、[`TrainingCardPoolScreen.tsx`](../src/features/week/TrainingCardPoolScreen.tsx) | 日常循環已具備等待、成本、衰退與連鎖取捨 |
| 每人獨立擲骰、實際增量與可重現種子 | [`TrainingCardResultDialog.tsx`](../src/features/week/TrainingCardResultDialog.tsx)、[`rng.ts`](../src/domain/rng.ts) | 回饋已補上個別結果與個性原因 |
| 隨機事件三種風險選項 | [`EventScreen.tsx`](../src/features/events/EventScreen.tsx)、[`events.ts`](../src/domain/events.ts) | 有取捨，但成功率固定且結果跳轉過快 |
| 5 先發＋3 輪替、三種建議陣容 | [`lineup.ts`](../src/domain/lineup.ts)、[`SeasonMatchScreen.tsx`](../src/features/season/SeasonMatchScreen.tsx) | 已具備球員起用決策 |
| 基準勝率、疲勞折損、傷病風險、戰術屬性預覽 | [`matchPreview.ts`](../src/domain/matchPreview.ts)、[`SeasonMatchScreen.tsx`](../src/features/season/SeasonMatchScreen.tsx) | 是目前最成熟的 UX 資產 |
| 上場角色影響疲勞與實戰成長 | [`officialMatch.ts`](../src/domain/officialMatch.ts) | 已符合兩款參考模式的核心原則 |
| 個性、球風、恢復差異 | [`types.ts`](../src/domain/types.ts)、[`matchEngine.ts`](../src/domain/matchEngine.ts)、[`styleTag.ts`](../src/domain/styleTag.ts) | 有差異，但部分仍只在公式內生效 |
| 賽後摘要、單季總結、生涯分享卡 | [`GameSummaryDialog.tsx`](../src/features/season/GameSummaryDialog.tsx)、[`SeasonSummaryDialog.tsx`](../src/features/season/SeasonSummaryDialog.tsx)、[`CareerSummaryScreen.tsx`](../src/features/career/CareerSummaryScreen.tsx) | 結果有收口，尚缺校史收藏層 |
| 單機自動存檔與格式驗證 | [`saveData.ts`](../src/domain/saveData.ts)、[`App.tsx`](../src/App.tsx) | 基礎可靠，UI 暫時隱藏匯入／匯出 |

### 6.2 本機畫面走查

本研究在 1280×720 桌面視窗實際走查開局、事件、一般週、球員詳情、功能選單與訓練骰子。應特別注意：內容殼本身限制為 520px，因此桌面截圖仍呈現手機版窄欄；這是產品選擇，不是錯誤。

觀察結果：

- **開局故事有氣氛**：退役球員轉任教練的文字能快速建立角色，但建隊後這條敘事線幾乎消失。
- **整體色彩一致**：深色模式下的黑褐底、米白字與橘色重點穩定；不同賽事階段、勝敗、危險與稀有內容的視覺語彙還不夠完整。
- **球員卡有辨識起點**：DiceBear 頭像讓 12 人不再只是名字，但風格較像社交頭像，沒有球衣、背號、位置動作與校隊世界觀。
- **六欄名冊資訊過密**：520px 寬時尚可辨識；更窄手機會讓姓名、位置、年級與總評縮得過小。球員卡也長駐在每個主畫面下方，會把主要決策推離視覺焦點。
- **訓練骰子是當時最有遊戲感的演出**：研究走查時 12 顆骰子依序停止有節奏，但結算只強調成功人數；後續卡池結果畫面已補上每名球員的實際增量與個性原因。
- **事件結果當時缺少停頓**：研究走查時點選後直接進下一週；後續已新增事件揭曉畫面。
- **選單只剩重新開始**：缺少賽程、球隊、校史、設定、存檔等穩定目的地，所有功能高度依賴目前頁面狀態。
- **動效可及性當時尚未補齊**：研究走查時骰子動畫沒有縮減動效替代，全域按鈕也缺少一致的 `:focus-visible`；後續已補上相關規則。[`index.css`](../src/index.css)、[`FatigueBar.css`](../src/features/roster/FatigueBar.css)

### 6.3 後續實作更新（2026-08-21）

- V1「每週都看得懂」已完成：四週行程帶、下一場提示、結果三段式、事件揭曉、名冊收合、`focus-visible` 與縮減動效均已落地。
- 舊每週三選一介面已由 9 張流動訓練卡池取代；每週可選最多 3 張，點數上限／回復隨聲望變化，卡片會衰退、逾期補新，同屬性卡可連鎖。
- 個別訓練已改為學習後天特殊能力；個性仍是天生固定的獨立系統。13 項能力已可學並接入比賽、傷病、疲勞、成長或恢復計算。
- 賽後摘要已可顯示最多三項主要原因與下一步建議，但尚未具備四節比分、BOX、領先曲線與關鍵回合資料。
- 最新完整驗證為 45 個測試檔、439 項測試通過、1 項略過；正式建置與靜態檢查通過。

## 7. 差距矩陣

| 面向 | 白球のキセキ | 栄冠ナイン | `chouten-court` 現況 | 最小正確方向 |
|---|---|---|---|---|
| 日常循環 | 練習連鎖、設備、起用、信賴 | 月度方向、進行圖示、事件格 | 9 張流動卡池、點數預算、最多 3 張、同屬性連鎖 | 補月度主題並驗證卡池平衡 |
| 球員差異 | 能力／特能直接強化戰術 | 能力、信賴、狀態影響戰術 | 天生個性＋13 項後天特殊能力 | 把個性／球風相性顯示在具體選項旁 |
| 比賽投入 | 多種操作深度、條件戰術 | 監督采配，最新版多種風格 | 賽前設定後直接出結果 | 快速結果＋關鍵回合＋文字實況 |
| 對手 | 評判、宿敵保護、憧憬球員 | 他校調查、宿敵編輯、舊隊回歸 | 強度值＋三年一換王牌 | 對手風格、偵察報告、可釘選宿敵 |
| 長期進程 | 無限世代、評判、設備、圖鑑 | 弱小到名門、短局接長局 | 聲望、18 屆上限、奪冠即結束 | 學校資產＋三年挑戰＋奪冠後可續玩 |
| 美術 | 寫實場館、群眾、聲音、數據 | 超變形角色、色碼、面板 | 卡片 UI＋隨機頭像 | 台灣校隊識別＋管理／比賽雙視覺節奏 |
| 結果回饋 | 戰績點、評價、圖鑑、選秀 | 評判、情緒、成長、事件 | 訓練／事件揭曉、勝敗原因與下一步 | 補人物焦點、BOX 與校史寫入 |
| 可重玩性 | 無限年數、宿敵與圖鑑 | 三年模式、轉任、舊隊宿敵 | 種子碼、單一長生涯 | 短局、歷史隊、挑戰規則 |

## 8. 優化建議：美術及畫面

### A1. 建立原創「台灣高中籃球」視覺聖經（P0）

不要追求寫實球員模型，先定義能反覆使用的五種視覺資產：

1. 球隊主色、輔色、校徽與球衣號碼樣式。
2. 非賽季、資格賽、預賽、複賽、八強、四強六種場館／光線主題。
3. 訓練、恢復、偵察、事件、比賽、危險六種穩定色碼與圖形符號。
4. 球員頭像外框：球衣、背號、位置、年級、傷疲與稀有標記。
5. 勝利、落敗、受傷、突破、畢業五種關鍵演出模板。

**驗收條件**：拿掉文字後，玩家仍能從顏色與輪廓區分目前是日常、正式賽、危險還是生涯里程碑；不得使用現實 HBL 校徽或未授權人物肖像。

### A2. 重整主畫面層級（P0）

建議主畫面只保留三層：

```text
固定 HUD：學校／年週／賽事／聲望
主要舞台：本週決策或本場比賽
可收合抽屜：名冊、賽程、校史、設定
```

- 名冊不再固定佔據每個畫面底部，改成 3–4 欄可收合抽屜或獨立頁。
- HUD 加入「距離下一場 3 週」與本月行程入口。
- 一般週、事件週與比賽週使用不同舞台背景，而不是同一張卡片只換文字。
- 桌面仍可維持 520–640px 的遊戲機面板，但背景可使用球館／校園氣氛，不必整頁空黑。

### A3. 升級球員視覺辨識（P1）

保留程式生成頭像的低成本優勢，但加入：

- 隊服色、背號與位置小圖示。
- 至少 4 種姿態：後衛、射手、側翼、內線。
- 傷勢、疲勞、火熱／低潮、隊長的輪廓級標記。
- 詳情頁顯示本季故事摘要，例如「近 3 場先發」「本月投籃 +4」「八強關鍵球員」。

角色的記憶點應優先來自球場角色與經歷，不是增加更多隨機臉部零件。

### A4. 為比賽建立「轉播層」而非 3D 層（P1）

低成本但高效的比賽畫面可以包含：

- 四節比分與領先曲線。
- 簡化半場圖／球場地板紋理。
- 兩校主色、校徽、先發五人剪影。
- 關鍵球員大卡與數據。
- 觀眾密度、燈光、彩帶與震動回饋隨賽事階段提高。

先做資訊演出，再評估 2D 動畫；沒有必要把專案轉成即時 3D 引擎。

### A5. 統一回饋與無障礙（P0）

- 所有按鈕提供 `hover`（僅支援 hover 的裝置）、`active`、`focus-visible`、`disabled`。
- 對骰子翻滾、閃光、對話框進出加入 `prefers-reduced-motion` 替代。
- 重要結果不只靠紅綠；搭配圖示、文字與形狀。
- 事件選擇後先揭曉成功／失敗、原因與數值，再由玩家前進下一週。
- 稀有里程碑可有較長演出；每週高頻操作維持快速、可跳過。

## 9. 優化建議：遊戲機制

### M1. 月度訓練方向＋三個個別指導名額（P0）

保留「一週一個主要決策」，避免把 12 名球員都變成微操作：

- 每四週選一個全隊方向，例如外線、防守、體能、傳導。
- 每月只能指定最多 3 名球員的個別目標。
- 連續兩週相關訓練可獲得明示連鎖加成。
- 個別目標與球員個性、位置相容時，在選項上直接顯示原因。
- 休養會中斷部分連鎖，但提供傷疲與狀態收益。

**驗收條件**：每週仍能在 10–20 秒內完成；玩家能在操作前說出本週選擇的收益、代價和受益球員。

### M2. 把個性與球風接到可用戰術（P1）

現有個性不要再增加純倍率，改成戰術相性：

| 現有個性 | 可見戰術影響示例 |
|---|---|
| 穩健型 | 降低「保住領先」的結果波動 |
| 天才型 | 個別指導時多一個可選成長方向 |
| 飆分型 | 強化追分／快速出手，但增加失誤或疲勞 |
| 隊長型 | 解鎖一次「重新集中」或提升暫停效果 |
| 抗壓型 | 關鍵回合出現專屬安全選項 |
| 玻璃體質 | 爆發選項收益較高，但畫面明示傷病風險 |

### M3. 關鍵回合教練模式（P1，高價值／高工作量）

每場比賽最多出現 2–4 次決策：

```text
第三節剩 3:10，落後 7 分，對方中鋒 4 犯。

[加速衝框]  飆分型相性＋／體力成本高
[錯位單打]  我方側翼優勢／成功率區間中
[穩住防守]  隊長型加成／追分速度慢
[交給助教]  立即模擬
```

設計底線：

- 選項由當場比分、時間、犯規、疲勞、陣容與球員特性生成。
- 顯示相性與風險，不顯示過度精確、會造成假確定感的成功率。
- 選擇影響後續模擬參數，不另寫一套勝負劇本。
- 玩家永遠可跳過並沿用目前全自動結果。

### M4. 對手風格、偵察與宿敵（P1）

在「強度＋王牌」上增加最小三層：

1. 校隊風格：快攻、外線、禁區、壓迫、防守紀律。
2. 偵察資訊：未偵察只見大方向；偵察後才見戰力區間、王牌弱點與常用戰術。
3. 宿敵紀錄：交手次數、歷史勝負、最大逆轉、經典球員；玩家可釘選重要程序生成學校。

宿敵必須跨年度保存；否則程序生成每三年刷新會抹掉玩家建立的情感。

### M5. 跨年度學校資產（P1）

依現有規格方向，第一版只做永久升級，不做耐久：

- 訓練館：提高特定訓練效率或開放第二個月度方向。
- 恢復中心：提高疲勞恢復預覽的穩定性。
- 球探網：增加可見候選數、縮小能力區間或解鎖其他地區。
- 影片分析：增加對手偵察資訊與戰術相性提示。
- 教練專長：每個生涯選一條可重置成本明確的發展路線。

每項資產都要改變選項或資訊，不要只提供不可感知的全隊 `+2%`。

### M6. 可解釋的狀態／士氣（P2）

目前先不要立刻把疲勞拆成多條。個別訓練與密集賽程落地後，再評估增加一個短期「狀態」：

- 疲勞：長期負荷與受傷風險。
- 狀態：近況、信心、上場節奏，數週內回歸中性。

狀態變動必須附原因，例如連勝、被冰、事件、休息或關鍵表現；不能成為另一條隨機黑箱。

### M7. 三年挑戰、奪冠後續與歷史隊（P2）

- 新增三年挑戰，讓首次完整體驗時間可控。
- 第三年結束可接回原本長生涯。
- 奪冠後讓玩家選擇「功成身退」或「繼續建立王朝」，不要強制結束唯一存檔。
- 將奪冠隊、最佳未奪冠隊與玩家指定隊伍保存成歷史隊，可在後續生涯作為宿敵或表演賽對手。

## 10. 優化建議：遊玩體驗

### X1. 永遠顯示下一個重要節點（P0）

HUD 或行程帶必須回答：

- 距離下一場正式賽幾週。
- 本月可用練習賽次數。
- 哪些球員在風險區。
- 下一個招生、畢業或賽事節點。

### X2. 結果畫面回答「發生什麼、為什麼、下一步」（P0）

所有結果統一成三段：

1. **發生什麼**：勝敗、成長、受傷、聲望。
2. **為什麼**：前三個主要因素與相性來源。
3. **下一步**：需要休養、換陣、偵察或繼續培養誰。

訓練骰子應顯示每人的實際增量與天賦加成；賽後摘要應增加四節比分、關鍵轉折與本場人物。

### X3. 新手引導採「第一個月任務」而非長篇教學（P1）

- 第 1 週：完成一次訓練並看懂骰子。
- 第 2 週：打開球員詳情並指定個別目標。
- 第 3 週：安排休養或練習賽，理解疲勞。
- 第一場正式賽：使用建議陣容後只改一名球員。

每一步都可略過；不要一次解釋整個 18 年生涯。

### X4. 快轉、設定記憶與多存檔（P2）

白球模式的官方更新顯示，長期循環會實際需要快轉、設定記憶與宿敵保護。[Ver.2.0.0 更新](https://www.konami.com/games/prospi/2024-2025/update/v200)

本專案建議：

- 記住最近訓練方向、陣容、戰術與結果顯示速度。
- 沒有事件／比賽的週可啟用快速重複，但在傷疲超標、個別目標完成或重要節點自動停下。
- 提供 3 個本機存檔槽＋自動存檔，不需要雲端。
- 重新開始、覆蓋與匯入前顯示明確存檔名稱與日期。

### X5. 收藏層取代服務型每日任務（P2）

建議收集：

- 歷屆隊伍與戰績。
- 球員三年成長曲線。
- 畢業去向與校史紀錄。
- 宿敵交手史。
- 經典比賽與分享卡。

不建議加入登入獎勵、能量、限時素材、強制連線或每日任務。這些是 Crossroad 的服務型節奏，不適合目前的單機核心。

## 11. 優先級與工作量

評分：影響 1–5；工作量 S／M／L／XL。工作量是相對現有 React／TypeScript 架構的估計，不是工期承諾。

| 順序 | 項目 | 影響 | 工作量 | 依賴 | 主要風險 |
|---:|---|---:|---|---|---|
| 1 | 結果原因、行程帶、按鈕／動效可及性 | 5 | M | 無 | 資訊過多，需漸進揭露 |
| 2 | 視覺聖經、主畫面層級、名冊抽屜 | 5 | M | 設計 token | 只換皮而未改善決策層級 |
| 3 | 月度方向＋3 個個別指導名額 | 5 | L | 行程帶 | 微操作膨脹、平衡值需測試 |
| 4 | 個性／球風連接戰術相性 | 4 | M | 結果原因 | 選項提示與公式不一致 |
| 5 | 對手風格、偵察、宿敵釘選 | 4 | L | 存檔版本升級 | 程序生成歷史資料膨脹 |
| 6 | 關鍵回合教練模式 | 5 | XL | 戰術相性、比賽事件模型 | 寫成第二套比賽引擎 |
| 7 | 學校永久資產（無耐久） | 5 | L | 聲望等級 | 升級只剩被動百分比 |
| 8 | 三年挑戰／奪冠後續／歷史隊 | 4 | L | 校史存檔 | 模式分支造成重複程式 |
| 9 | 球員視覺升級與比賽轉播層 | 4 | L | 視覺聖經 | 資產量與效能失控 |
| 10 | 可選音效／環境聲／大賽演出 | 3 | M | 轉播層 | 自動播放、音量與版權問題 |

## 12. 建議實作路線：四個可獨立交付的版本

目前總覽：**基礎 MVP、V1、V2、V3、V4 皆已完成。**四個版本的規劃項目已全數落地(部分項目依文件記載的範圍決策做了刻意縮小,細節見各版本段落的補充說明)。

### V1：每週都看得懂（已完成）

- [x] 四週行程帶與下一重要節點。
- [x] 統一結果三段式。
- [x] 訓練顯示實際增量與個性原因。
- [x] 事件揭曉畫面。
- [x] `focus-visible`、縮減動效、非紅綠單一辨識。
- [x] 名冊改為可收合入口。

**成功指標**：新玩家不開規格文件，也能說明疲勞、實戰成長與下一場時間。

### V2：每名球員都有培養理由（進行中）

- [x] 月度訓練方向(輕量版:即時折扣提示,非玩家手動選擇,見下方補充)。
- [x] 每週最多 3 張訓練卡，個別訓練可指定球員與後天特殊能力。
- [x] 同屬性全隊訓練連鎖。
- [x] 個性／球風相性提示。
- [x] 球員近況與本季故事摘要(MVP:即時近況,非持久化本季事件簿,見下方補充)。

補充：流動卡池與特殊能力是原 V2 路線確立後新增的深化方案，已實作完成；它們提高了培養分歧，但不等同於尚未完成的「月度方向」或固定三名指導名額。

補充(2026-08-22)：月度訓練方向以「本月焦點」輕量版落地——不新增選擇畫面、不寫入存檔。新增 `trainingDirection.ts` 的 `computeTeamFocusStyle(players)`,即時從當下名冊算出 `styleTag.primary` 人數最多的球風分類(同分依 `STYLE_KEYS` 固定順序決定,穩定不跳動);對應屬性(透過既有 `primaryStyleForAttribute`)的全隊訓練卡拿到 `FOCUS_DISCOUNT=1` 點固定折扣,`trainingCardPool.ts` 的 `cardCost`/`totalCost`/`canSelectCards` 新增選填 `focusStyle` 參數(預設 `null`,舊呼叫端行為不變)。`App.tsx` 扣點與 `TrainingCardPoolScreen.tsx` 顯示/選卡都用同一個 `team.players`/`players` 算出的焦點,確保畫面折扣與實際扣點一致。名冊變動(訓練成長、招生、畢業)時焦點會自然跟著重算,不需要額外狀態,因此**不需要 bump `SAVE_FORMAT_VERSION`**。

補充(2026-08-22)：名冊詳細彈窗新增「近況」一句話摘要(新增 `playerStatus.ts` 的 `describePlayerStatus`)，依傷勢／疲勞／目前最強屬性／個性語氣即時合成，不需要新增持久化欄位、不動存檔格式。**範圍刻意縮小**：這是「即時狀態」而非真正跨週累積的「本季故事」(例如某週練習大成功、某場比賽關鍵表現)——完整的本季事件簿需要新增每週事件紀錄的持久化資料結構與存檔格式升版，屬於較大工作量，本次先以零額外狀態的版本驗證呈現方式是否有感，後續如果需要跨週敘事再評估加上事件簿。

補充（2026-08-22）：卡池畫面新增個性／球風相性提示——全隊訓練卡會顯示「有固定加成的個性＋人數」（沿用既有 `genius`／`scorer` 訓練加成規則，來源統一至 `weeklyAction.ts` 的 `attributeAffinityPersonalities`）與「球風契合人數」（`styleTag.ts` 新增 `primaryStyleForAttribute`，取該屬性在球風權重表中係數最高的分類，判斷球員是否已是同球風主傾向）。純 UI／唯讀衍生資訊，未改動任何成長或機率邏輯。

**成功指標**：相同名冊下，不同玩家能形成至少兩種合理培養路線，而不是永遠選同一屬性。

### V3：比賽真的需要教練（已完成）

- [x] 對手風格與偵察(資訊層,見下方補充;戰術相性仍是 M2 的未來範圍)。
- [x] 四節比分、BOX(引擎版四節+個人得分/籃板/助攻,見下方補充;領先曲線/關鍵球員仍未做)。
- [x] 2–4 次關鍵回合決策(真互動版,見下方補充)。
- [x] 快速結果／關鍵回合兩種速度(隨時可按「快速結果」跳過剩餘節數與決策)。
- [x] 勝敗最多三項原因與下一步建議。

補充(2026-08-22)：新增 `opponentStyle.ts`——對手校隊風格(快攻/外線/禁區/壓迫/防守紀律)沿用 `opponentAce.ts` 同屆固定、換屆重生的種子模式,純敘事資訊,**不影響戰力或勝率計算**(校隊風格真正接上戰術相性/戰力調整是 M2 的範圍,這裡先只做資訊層,降低風險)。新增「偵察」門檻:聲望達到 `SCOUTING_REPUTATION_THRESHOLD`(=55)才視為已偵察,呼應招生/特殊能力既有的聲望解鎖慣例,不需要新增存檔欄位。已偵察時額外顯示對手戰力的模糊區間(`scoutedStrengthRange`,真實值前後各 4 分,不直接洩漏精確數字)與王牌弱點一句話描述(`describeAceWeakness`,比較王牌自己的得分/三分兩項屬性)。`SeasonMatchScreen.tsx` 賽前資訊卡新增對應區塊。

補充(2026-08-22)：四節比分改以**引擎版**落地(討論後放棄了風險較小的敘事版,因為那個版本事後生成分數、不參與模擬過程,之後做「關鍵回合決策」時無法在真實的節/時間點插入互動)。新增 `quarterSimulation.ts` 的 `simulateQuarters(teamStrength, opponentStrength, varianceRange, rng)`,取代 `officialMatch.ts` 的 `simulateOfficialGame` 原本呼叫 `computeMatchWinProbability` 的單次擲骰(`rng() < winProbability`):現在改成真的跑 4 節,每節重新對我方戰力疊加一次隨機噪音(沿用 `computePerformanceVarianceRange` 決定的波動範圍)換算成分差,加總四節分數,**由四節總分決定輸贏**,不再是機率骰;極少數分數打平的情況用一次額外的銅板 rng() 決定勝方,不重擲整場。單場 rng() 消耗量從原本固定 2 次變成固定 12 次(平手時 13 次),但完全不影響既有測試——`officialMatch.test.ts`/`App.test.tsx` 原本就用「跑很多個 seed 找出符合條件的一個」或「跑大量 seed 驗證統計趨勢」的寫法,沒有任何測試假設特定 seed 對應特定輸贏,所以全數維持通過。`OfficialGameResult`/`AdvanceSeasonWeekResult`/`GameSummaryResult` 新增 `boxScore: { quarters, final }` 欄位一路透傳到 `GameSummaryDialog.tsx` 顯示逐節比分。**刻意縮小範圍**:只有正式賽(`simulateOfficialGame`)換成引擎版;練習賽(`weeklyAction.ts` 的 `applyPracticeMatch`)與練習賽訓練卡(`trainingCardResolution.ts`)維持原本的單次擲骰,`computeMatchWinProbability`/`computeWinProbability` 兩個函式都還在用、沒有刪除。賽前預覽(`matchPreview.ts` 的「基準勝率」)也維持不動,概念上仍是「開打前的機率估計」,跟正式開打後的引擎式逐節結果本來就是兩套獨立的呈現。

補充(2026-08-22)：關鍵回合決策採**真互動版**——玩家的選擇真的會改變剩下比賽的模擬參數,不是事後標記的敘事。核心是把原本一次算完 4 節的 `simulateOfficialGame` 拆成三段:`setupOfficialGameQuarters`(算好戰力/波動範圍與這場比賽專屬的 rng)、`quarterSimulation.ts` 新增的 `simulateOneQuarter`(單節模擬,可傳入 `QuarterModifier` 疊加分差加成與波動倍率)、`resolveOfficialGameAfterQuarters`(四節結束後接續同一個 rng 做疲勞/受傷/成長結算)。`simulateOfficialGame` 仍保留,只是變成呼叫這三段的便利包裝(全自動、無決策),對外行為完全不變。新增 `keyMoments.ts`:分差在 ±6 分內(原創數值,待調校)才視為「膠著」,在第 1~3 節結束後(0-based `isKeyMomentTrigger`)可能跳出決策,最後一節結束後不再詢問;四個選項(加速反攻/穩紮穩打/收縮防守/交給球員自由發揮)各自對應不同的 `marginBonus`/`varianceMultiplier`,`auto` 選項的效果完全等於中性(不套用任何加成),數學上跟全自動模擬一致。新增 `KeyMomentGameScreen.tsx`:用 `useRef` 持有這場比賽專屬的 rng 與戰力參數(在 React render 之間保持同一個有狀態的 rng 實例),`useEffect` 鏈自動逐節推進,分差夠接近時暫停跳出決策 UI;玩家隨時可以按「快速結果」跳過剩餘節數與所有決策,直接用中性參數把剩下的節數模擬完——這就是「快速結果／關鍵回合兩種速度」的實作,兩種路徑最終都走同一套 `resolveOfficialGameAfterQuarters` 收尾。**架構調整**:`season.ts` 的 `advanceSeasonWeek` 不再自己呼叫 `simulateOfficialGame`,改成接手「已經算好的 `OfficialGameResult`」——因為現在比賽結果可能來自全自動一次算完,也可能來自玩家逐節互動,`season.ts` 不需要也不該關心過程,只做戰績/晉級簿記。`App.tsx` 新增 `team.livePlay` 這個純執行期狀態(不寫入存檔,離開/重整會直接放棄這場進行到一半的比賽,回到賽前畫面重新開打),點擊「開打」先鎖定戰術/陣容進入 `KeyMomentGameScreen`,比賽完成後才呼叫 `advanceSeasonWeek` 走原本的賽季推進流程。

補充(2026-08-22)：只有逐節團隊比分,沒有個人數據(得分/籃板/助攻)會不夠真實,因此追加 BOX——新增 `boxScoreStats.ts` 的 `distributePlayerStats(teamPoints, roster, lineup, rng)`,把 `simulateQuarters` 算出的官方全隊得分,依出賽權重(沿用 `lineup.ts` 既有的先發6:輪替3 戰力加權)與對應屬性(得分看投籃/三分平均、籃板看籃板、助攻看傳球)分配給每位先發/輪替球員,搭配 ±15% 抖動避免數據精準跟屬性成正比;用「最大餘數法」取整數,確保個人得分加總後精確等於官方全隊得分。籃板/助攻的全隊總量是獨立的原創基準值(34±6、15±4),不受官方比分限制。**刻意縮小範圍,只做全場總計**:不分節,單純是統計式的事後分配,不是真正逐節都各自模擬一次個人數據;用獨立種子(`hashSeed('...boxstats')`)呼叫,完全不影響 `simulateOfficialGame`/`season.ts` 既有的 rng 消耗順序與測試。`GameSummaryDialog.tsx` 的「先發與主要輪替」清單新增得分/籃板/助攻,不另開一個獨立區塊。

**成功指標**：改變陣容或戰術會改變可用的關鍵選項，玩家能理解但不能完全算死結果。

### V4：建立一所會被記住的學校（已完成）

- [x] 永久學校資產(見下方補充;聲望仍是連續值,未新增離散等級)。
- [x] 宿敵、交手史與釘選(見下方補充)。
- [x] 校史、歷史隊(見下方補充;球員成長曲線縮小為畢業/奪冠快照,非逐週時間序列)。
- [x] 三年挑戰可接長局(見下方補充)。
- [x] 奪冠後退休／繼續王朝(見下方補充)。

補充（2026-08-22）：新增 `rivals.ts`——對手校名原本每週純隨機生成、沒有任何身分延續（`opponentNameForWeek` 每次都重新擲名字），現在玩家可在賽前資訊卡把當週對手「釘選為宿敵」（最多 `MAX_PINNED_RIVALS=3` 所，原創數值，待調校），釘選後這所學校的名字會被記住，`pickOpponentName` 讓已釘選宿敵有 `RIVAL_MATCHUP_CHANCE=35%`（原創數值，待調校）機率成為本場對手，取代原本的隨機校名；每次交手依賽果累加勝負，並用 `computeComebackMargin` 從已經算好的四節比分事後推算「我方最大逆轉分差」（曾經落後過的最大分差，贏球才算）。**跨屆保存**是這個設計的重點：宿敵資料存在 `team.rivals`（存檔版本升到 11，舊存檔會被拒絕，無 migration，符合專案既有慣例），不會因為換屆招生/畢業而重置。`SeasonMatchScreen` 賽前資訊卡在對手是宿敵時顯示「★宿敵・交手 X勝Y敗・最大逆轉 Z分」與取消宿敵按鈕；不是宿敵且清單未滿時顯示「釘選為宿敵」按鈕。**刻意縮小範圍**：只做「釘選學校 + 累積交手戰績」，沒有做校史/歷史隊/經典球員這類更重的資料模型（那些留給校史/歷史隊項目）。

補充（2026-08-22）：新增 `schoolAssets.ts`——聲望達到門檻永久解鎖對應資產,不會因聲望之後下降而收回,呼應 M5「永久學校資產」的設計精神(第一版只做永久升級,不做耐久)。目前實作 2 項,都直接改變選項或資訊、不是不可感知的全隊 `+2%`(M5 的明確要求):**球探網**(聲望 ≥60,原創數值,待調校)——招生候選池從 `vacancies*2` 翻倍成 `vacancies*4`,直接改變招生的選才空間;**影片分析室**(聲望 ≥70,原創數值,待調校)——不論當下聲望高低,永遠視為已偵察對手(繞過 `opponentStyle.ts` 的 `SCOUTING_REPUTATION_THRESHOLD`)。解鎖當下會在賽後摘要/事件結果訊息附加一句「永久解鎖學校資產:OOO!」,`AppShell` 標題列固定顯示「永久資產:OOO」清單。存檔版本升到 12(舊存檔會被拒絕,無 migration,符合既有慣例)。**刻意縮小範圍**:規格原列 5 項資產(訓練館/恢復中心/球探網/影片分析/教練專長),這次只做「改變單一選項/資訊」型的兩項;訓練館(第二個月度方向)、恢復中心(疲勞恢復穩定性,偏向不可感知)、教練專長(整套發展路線系統)工作量明顯更大,留給後續(2026-08-23 補上,見本節 V5 補充)。聲望仍是連續值,沒有引入白球式的離散評判等級。

補充（2026-08-22）：新增 `schoolHistory.ts`，用**獨立於單一生涯存檔的 localStorage 鍵**保存跨生涯的校史紀錄——這是刻意的架構決策：「開始新生涯」清除的是 `saveData.ts` 的單一生涯進度，校史必須活在那之外才有意義。生涯結束(奪冠/屆滿/三年挑戰結束)當下組出一筆 `SchoolHistoryEntry`(教練名、結束原因、生涯戰績、最佳戰績、只有奪冠生涯才附的「歷史隊」陣容快照)寫入校史，`SetupScreen` 開局畫面讀出並列出所有過往生涯，故事文案也會依是否有校史切換措辭。**球員成長曲線刻意縮小範圍**：只保留奪冠隊當下的屬性快照(`computeOverallGrade` 綜合評級)，不是逐週時間序列圖表——真正的成長曲線需要對每位球員做多時間點的屬性快照，資料量與工作量明顯更大，這次先驗證「校史本身有沒有感」。

補充（2026-08-22）：新增「三年挑戰」執教模式(`career.ts` 的 `hasReachedShortChallengeMilestone`，門檻 `SHORT_CHALLENGE_ERAS=3`，原創數值待調校)，呼應白球のキセキ／栄冠ナイン系列「短局結束後可接長局」的設計。`SetupScreen` 開局新增模式選擇(長期生涯/三年挑戰)；短局模式下 `eraCount` 跨過門檻時跳出非阻斷式的 `ChallengeDecisionDialog`，玩家可選「繼續帶下去」(不再詢問，等同轉為長期生涯)或「在此結束」(立即寫入校史，生涯正常結束)。**刻意採非阻斷式設計**：跳出提示的當下，招生/訓練等其他畫面仍可正常操作，不強迫玩家立刻決定；換來的代價是若玩家拖了幾週才點開，寫進校史的戰績會是「點擊當下」而非「跨過門檻當下」，兩者通常只差一兩週，用敘事寬容度換取實作簡單度。

補充（2026-08-22）：奪冠不再自動結束生涯的「唯一路徑」——`CareerSummaryScreen` 新增「不退休，繼續帶下去(王朝模式)」按鈕(只在 `reason==='champion'` 時顯示)。**刻意選擇風險最低的實作路徑**：沒有更動比賽結算當下(`onComplete` 內)的既有邏輯——奪冠依然立即結束生涯、立即寫入校史，跟長期生涯模式的既有測試行為完全一致；「王朝」按鈕點下去時才**額外**在 `CareerSummaryScreen` 這一層補做原本奪冠會跳過的畢業/招生流程(`advanceGrades`＋條件式招生候選池)，然後把 `careerEnded` 設回 `null`，讓遊戲直接接續下一季——`team.totalWeek`／`reputation`／`careerLog` 在確認賽後摘要時就已經是「下一季開局」的正確值，不需要重算。若王朝模式下又達到保險上限(`eraCount` 達 18)，會照樣寫入校史結束生涯，不會無限循環下去。存檔版本因新增 `rivals`／`schoolAssets`／`challengeMode`／`pendingChallengeDecision` 四個欄位，從 10 一路升到 13(舊存檔會被拒絕，無 migration，符合既有慣例)。

**成功指標**：即使某屆早早淘汰，玩家仍能指出這屆對學校或後續生涯留下了什麼。

### V5：深入研究後的補強項目（進行中）

2026-08-22 對整個專案做了一輪「還有什麼可以改進」的深入研究，找出的缺口逐項補齊，完成一項就在下方補充一段。跳過的項目（雲端同步、BGM/音效、既有 49 個「原創數值,待調校」的平衡調校）已經跟使用者確認過範圍,不在這輪清單內。

- [x] 存檔格式升級改用漸進式遷移鏈,不再是「版本不符就整包拒絕」(見上方 `saveData.ts` 的 migration 說明)。
- [x] 賽後/球季總結彈窗疊加(dialog stacking)問題修掉,`SeasonSummaryDialog` 新增 `onClose`,`ChallengeDecisionDialog` 改成等玩家關掉球季總結才跳出。
- [x] 補上 `@vitest/coverage-v8` 測試覆蓋率工具(`npm run test:coverage`)。
- [x] `App.tsx` 的球季/正式賽收尾邏輯抽成 `officialGameWeek.ts`,純函式、獨立測試,`App.tsx` 只留orchestration。
- [x] 對手完整名冊(見下方補充)。
- [x] 本機多存檔槽位(見下方補充)。
- [x] 永久傷病後遺症(見下方補充;同步修訂了 `docs/spec.md` 第 11 節原本「無永久後遺症」的規劃決策)。
- [x] 成就/稱號系統(見下方補充;`docs/spec.md` 第 17 節從「MVP 尚未實作」改為已實作)。
- [x] 訓練館/恢復中心/教練專精三項學校資產(見下方補充,補齊 M5 原列 5 項資產中先前跳過的 3 項)。
- [x] 先發位置涵蓋軟性懲罰(見下方補充;第 7 節「位置組合僅警告」的規劃決策首次真正影響戰力)。

補充（2026-08-23）：新增 `opponentRoster.ts` 的 `generateOpponentRoster(seed, size)`,包裝既有的 `createInitialRoster` 產生一份跟我方名冊同規格的完整對手名冊。`SeasonMatchScreen` 在已偵察(`isOpponentScouted(reputation)` 或永久解鎖影片分析室)時,新增一個可展開的「對手名冊」清單,顯示每位球員的姓名/位置/年級評等。**刻意縮小範圍**:純資訊展示,不影響戰力計算——正式賽勝負仍由 `matchEngine.ts` 既有的戰力/戰術模型決定,對手名冊不是「真的在場上打球的 12 人」,只是給玩家多一點資訊感。

補充（2026-08-23）：新增本機多存檔槽位,取代舊版「固定一個 localStorage key、只能存一份」的限制。`saveData.ts` 新增 `listSaveSlots`/`createSaveSlot`/`loadSaveFromSlot`/`writeSaveToSlot`/`deleteSaveSlot`/`readActiveSlotId`/`writeActiveSlotId`,每個槽位各自一個 storage key,另有一個索引 key 記錄所有槽位的名稱與最後更新時間。`migrateLegacySingleSlotSave` 在「還沒有任何槽位」時自動把舊版單一存檔搬進新系統,讓既有玩家的進度不受影響(這也是既有測試不需要大改的原因——舊測試直接寫入舊版 key,啟動時會被自動遷移)。新增 `SaveSlotScreen.tsx`:沒有作用中存檔但已有槽位時顯示存檔清單(可載入/可刪除),沒有任何槽位時直接進 `SetupScreen`(維持原本首次進入的體驗)。`SaveControls` 新增「切換存檔」按鈕,回到存檔選單但不刪除任何資料;既有的「重新開始」維持原本語意(刪除目前這個槽位,不是單純切換)。**刻意縮小範圍**:沒有做槽位數量上限(X4 規格建議 3 個,這裡選擇不設上限,因為本機儲存空間對這種文字/JSON 存檔量級不是實際限制)、沒有做槽位改名 UI(建立時用「校名・教練名」當預設標籤,足以區分)、也沒有做 X4 一併提到的「快轉」與「設定記憶」(訓練方向/陣容/戰術記憶)——那些是獨立的體驗改善,不屬於「多存檔」本身。

補充（2026-08-23）：新增永久傷病後遺症——重傷球員經過「重傷缺賽→復出過渡期屬性打折→痊癒」全程後,`matchEngine.ts` 的 `tickInjuryRecovery` 在過渡期倒數結束的那一刻額外滾一次 `PERMANENT_AFTEREFFECT_CHANCE=0.2`(原創數值,待調校)機率,命中則隨機一項屬性永久 `-1`(`clamp` 到 0 下限,不會扣成負數)。輕傷不會觸發——輕傷從不進入復出過渡期,直接痊癒,跟原本規則一致。新增 `describePermanentAftereffects(before, after)`:純靠「康復前是 returning、康復後是 healthy、且有屬性數值下降」這個條件推斷是否剛好發生了後遺症,不需要在 `Player` 型別新增額外欄位、也不需要升級存檔版本——同一週唯一會讓屬性下降的來源就是這個機制,不會跟訓練成長(只會 +)搞混。這個描述函式接到兩個既有的收尾路徑:正式賽收尾(`officialGameWeek.ts` 的 `resolveOfficialGameWeek`)與訓練週收尾(`App.tsx` 的 `runTrainingCardWeek`),因為「過渡期倒數」本身只跟週數有關、跟當週是否有比賽無關。**刻意縮小範圍**:沒有做「耐久度」或「未來受傷機率上升」這類疊加機制(那類設計會跟玻璃體質/玄鐵之軀既有的受傷機率係數混在一起,語意複雜化),只做規格原先提到的「小幅永久屬性下降」這個最單純的版本。

補充（2026-08-23）：新增 `achievements.ts`,實作 spec.md 第 17 節原本「MVP 尚未實作」的成就/稱號系統,純紀錄性質,不影響任何數值運算。目前 4 項(原創定義,待調校):**全勝賽季**(單季所有正式賽全勝)、**零傷賽季**(單季完全沒有球員新增傷勢——用新增的 `Team.seasonHadInjury` 布林值全季追蹤,每季結束後重置,因為傷勢可能發生在訓練週的練習賽,不只發生在正式賽週)、**大逆轉**(單場正式賽最大逆轉分差達到 `COMEBACK_WIN_MARGIN_THRESHOLD=15` 分後獲勝,重用既有的 `rivals.ts` 的 `computeComebackMargin`)、**連霸八強**(`careerLog` 最近兩屆賽季的 `finalPhaseReached` 都達到八強賽或以上)。成就只會累積、不會被收回,解鎖當下在賽後訊息附加「解鎖成就:OOO!」,生涯總結畫面(`CareerSummaryScreen.tsx`)新增區塊列出所有已解鎖成就與說明。存檔版本升到 14(新增 `achievements`/`seasonHadInjury` 兩個欄位,migration 13→14 補上預設值 `[]`/`false`,舊存檔可正常升級)。**刻意縮小範圍**:只做 4 項最能直接從既有資料(`SeasonRecord`/`boxScore.quarters`)推導的成就,沒有做需要額外資料模型的項目(例如「生涯總得分累積」「特定球員連續先發場次」);也沒有做成就的稀有度分級或展示動畫,純粹是一個名稱+一句說明的清單。

補充（2026-08-23）：補齊 M5 原列 5 項學校資產中先前跳過的 3 項,`SCHOOL_ASSET_KEYS` 從 2 項擴到 5 項,每項都直接改變一個玩家可見的選項或數字,不是不可感知的全隊 `+2%`:**訓練館**(聲望 ≥55,原創數值,待調校)——`trainingCardPool.ts` 的 `canSelectCards` 新增可選的 `maxCards` 參數(預設沿用既有的 `MAX_CARDS_PER_WEEK=3`),解鎖時 `App.tsx` 傳入 `MAX_CARDS_PER_WEEK+1`,`TrainingCardPoolScreen` 每週最多可選卡數變成 4 張,是直接可見的選項增加。**恢復中心**(聲望 ≥65)——`matchEngine.ts` 的 `computeRecoveryRate`/`applyFatigueDelta`/`advancePlayerWeek` 新增可選的 `recoveryBonus`/`schoolBonus` 參數(預設 0,不影響任何既有呼叫),解鎖時全隊每週體力恢復量 `+RECOVERY_CENTER_BONUS=2`(原創數值,待調校),數字直接反映在名冊畫面「每週體力恢復」的顯示上,正式賽(`officialMatch.ts` 的 `resolveOfficialGameAfterQuarters`/`simulateOfficialGame`)與訓練週(`trainingCardResolution.ts` 的 `resolveCardSelections`)都吃得到這個加成。**教練專精**(聲望 ≥75)——`specialAbilities.ts` 的 `unlockedAbilityCount`/`unlockedAbilities`/`learnableAbilitiesForPlayer` 新增可選的 `bonusSlots` 參數(預設 0),解鎖時個別訓練可教學的特殊能力清單多解鎖 1 項(仍受 `MAX_UNLOCKED_ABILITIES` 總量上限保護,不會超過完整能力表)。三項全部走「新增參數、預設值等於過去行為」的做法,舊有呼叫端與測試完全不用改。存檔版本沿用 14,不需要升版——因為 `schoolAssets` 陣列早在 V4 就已經是存檔欄位,新增可能的 key 值不影響既有存檔的解析。

補充（2026-08-23）：新增先發位置涵蓋軟性懲罰,回應「上場人員是否要依位置強制一人一位,或上錯位置給予懲罰」的提問。研究後判斷:強制五個位置各一人比較貼近棒球「一人一守位」的硬規則,籃球本身允許小球/雙塔等非典型陣容,而且會重現 `lineup.ts` 原本就刻意避開的風險——傷病潮時可能湊不出合法先發,直接卡關;因此採用比使用者原提兩案都更折衷的方案:**軟性戰力懲罰**。新增 `lineup.ts` 的 `countStarterPositionMismatches(roster, starters)`:只看先發五人(不含輪替/未上場),PG/SG/SF/PF/C 五個位置中,每個「完全缺席」或「重複超過一人」的位置各算一次問題;`positionMismatchMultiplier(mismatchCount)` 讓隊伍有效戰力依問題數打折,每次 `POSITION_MISMATCH_PENALTY=0.03`(原創數值,待調校),下限 0。`matchEngine.ts` 的 `computeTeamStrength` 在算完先發/輪替加權平均後、加上隊長等一次性團隊加成前,乘上這個倍率——先發五人剛好一人一位置時倍率是 1,完全不影響任何既有測試(`createInitialRoster` 保證名冊前 5 人天生涵蓋五個不同位置,大多數既有測試的 5 人先發因此本來就不觸發懲罰)。`matchPreview.ts` 的 `MatchPreview` 新增 `positionMismatchCount` 欄位,`SeasonMatchScreen.tsx` 在「目前戰力」旁顯示具體折損百分比,並在既有的位置組合警告區塊下方加一行明確說明「先發五人有 N 個位置缺席或重複」。**刻意不做**:沒有把 `analyzeLineupComposition` 既有的「缺主力持球者/缺內線/過度集中」三個категорical 警告改掉或合併,兩套機制並存——舊的是對全部 8 個上場名額的粗略分類提示,新的是對先發 5 人的精確位置涵蓋計算,語意不同,沒有必要二選一,分類提示保留給想看更細診斷的玩家參考。也沒有做位置槽位指派 UI(仍是自由選 5 人,不用逐一指定「這位打 PG、那位打 C」),維持專案一貫的最小可行 UI。

## 13. 明確不建議現在做

1. **完整 3D 比賽**：成本與現有純前端手機優先方向不相稱。
2. **完整 12–20 人對手名冊**：在偵察與宿敵尚未成立前，資料量不會自動產生策略。
3. **設備耐久**：沒有預算、維修與替代方案前，只會增加例行雜務。
4. **同時拆成體力、疲勞、士氣、狀態、信賴五條**：先讓個別訓練和關鍵比賽證明需要。
5. **現實球員肖像或校隊資料**：涉及授權且會削弱原創世界。
6. **Crossroad 式能量與每日任務**：不符合單機、可離線與玩家自主節奏。
7. **大量永久被動加成**：長期資產必須解鎖新選項、資訊或規則。
8. **把所有建議一次實作**：先以 V1 驗證資訊與節奏，再增加系統深度。

## 14. 最終產品方向

`chouten-court` 不應成為「棒球模式換成籃球數值」，也不需要在寫實度上和《野球魂》競爭。更適合的定位是：

> 一款手機優先、決策透明、帶台灣高中籃球氣氛的世代球隊經營遊戲；平日像《栄冠ナイン》一樣快速可讀，大賽像「白球のキセキ」一樣讓球員、場館與關鍵決策被記住。

最重要的差異化不是新增更多系統，而是把現有的訓練、疲勞、個性、陣容、賽事與畢業串成可見因果：

```text
我為什麼培養他
→ 我為什麼在這場用他
→ 他如何改變我的選擇
→ 這一屆離開後留下了什麼
```

只要這四句在每個生涯都能得到不同答案，專案就會從「功能完整的模擬器」前進到「有故事、有執教感、值得重玩的遊戲」。

## 15. 第一方外部來源

### 白球のキセキ

- [《プロ野球スピリッツ2024-2025》官方網站](https://www.konami.com/games/prospi/2024-2025/)
- [《プロ野球スピリッツ2026》官方網站](https://www.konami.com/games/prospi/2026/jp/ja/)
- [Ver.1.1.0 更新](https://www.konami.com/games/prospi/2024-2025/update/v110)
- [Ver.1.2.0 更新](https://www.konami.com/games/prospi/2024-2025/update/v120)
- [Ver.2.0.0 更新](https://www.konami.com/games/prospi/2024-2025/update/v200)
- [白球模式 FAQ 一覽](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/category?category=9)
- [信賴度 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=136)
- [球探評價 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=135)
- [練習指令加成 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=142)
- [戰術卡相性 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=149)
- [練習器材耐久 FAQ](https://www.konami.com/games/prospi/2024-2025_support/faq/0/jp/ja/pc/item?no=145)
- [2026 DH FAQ](https://www.konami.com/games/2026_support/faq/0/jp/ja/ps5/item?no=264)
- [2026 連投與恢復 FAQ](https://www.konami.com/games/2026_support/faq/0/jp/ja/ps5/item?no=266)

### 栄冠ナイン

- [《パワフルプロ野球2024-2025》栄冠ナイン官方模式頁](https://www.konami.com/pawa/2024-2025/mode/eikan)
- [《パワフルプロ野球2026-2027》栄冠ナイン官方模式頁](https://www.konami.com/pawa/2026-2027/mode/eikan)
- [2024-10-21 第五次更新](https://www.konami.com/pawa/2024-2025/update/241021)
- [《パワプロ2020》官方開發前夜祭：栄冠ナイン篇](https://www.konami.com/pawa/2020/development/timeline_4)
- [《栄冠ナイン クロスロード》官方發表](https://www.konami.com/games/corporate/ja/news/topics/20230803/)
- [Crossroad「世界の栄冠」官方更新](https://www.konami.com/games/corporate/ja/news/topics/20240313/)

### 專案內部基準

- [完整設計規格](spec.md)
- [既有白球模式研究](prospi-hakkyuu-no-kiseki-gameplay-analysis.md)
- [既有籃球遊戲性改善計畫](basketball-gameplay-improvement-plan.md)
- [UI 畫面草圖](ui-wireframes.md)
