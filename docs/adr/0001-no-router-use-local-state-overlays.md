# 沒有路由套件，次要畫面用本地 state 疊層呈現

目前應用程式沒有引入任何路由套件；`AppShell` 用元件內 `useState` 開關疊層畫面(現有的存檔選單「⋯」)。V1 為名冊新增獨立收合按鈕時,延續同一種模式,而不是引入路由函式庫。這是刻意選擇:目前應用程式以單一畫面瀏覽為主,路由所帶來的分頁狀態與瀏覽器歷史整合複雜度在此階段不成比例。若之後疊層畫面數量明顯增加(例如校史、賽程總覽等長期功能),需要重新評估是否導入路由。
